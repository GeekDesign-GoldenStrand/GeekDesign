name: CI

on:
  push:
    branches:
      - main
      - develop
      - "feature/**"
      - "fix/**"
      - "chore/**"
      - "refactor/**"
      - "test/**"
  pull_request:
    branches:
      - main
      - develop

defaults:
  run:
    working-directory: portal-geek

jobs:
  # ── Fast feedback — runs on every push and PR ────────────────────────────────

  lint:
    name: Lint
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: portal-geek/package-lock.json
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Generate Prisma Client
        run: npx prisma generate
      - name: Run lint
        run: npm run lint

  format:
    name: Format
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: portal-geek/package-lock.json
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Check format
        run: npm run format:check

  typecheck:
    name: Typecheck
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: portal-geek/package-lock.json
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Generate Prisma Client
        run: npx prisma generate
      - name: Run typecheck
        run: npx tsc --noEmit

  test:
    name: Tests
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: portal-geek/package-lock.json
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Generate Prisma Client
        run: npx prisma generate
      - name: Run tests
        run: npm test -- --ci

  # ── Heavier checks — only on PRs to main or develop ─────────────────────────

  build:
    name: Build
    runs-on: ubuntu-22.04
    if: github.event_name == 'pull_request'
    needs: [lint, format, typecheck, test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: portal-geek/package-lock.json
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Generate Prisma Client
        run: npx prisma generate
      - name: Run build
        run: npm run build
        env:
          DATABASE_URL: postgresql://dummy:dummy@localhost:5432/dummy
          AUTH_SECRET: ci-dummy-auth-secret-not-used-in-build-1234
          RESEND_API_KEY: re_ci_dummy_key_not_used_in_build
          RESEND_FROM_EMAIL: noreply@geekdesign.mx

  links:
    name: Link Check
    runs-on: ubuntu-22.04
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
      - name: Check links in markdown files
        uses: lycheeverse/lychee-action@v2
        with:
          args: >
            --verbose
            --no-progress
            --exclude-loopback
            --exclude "^mailto:.*"
            --exclude "^https?://localhost"
            "**/*.md"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}