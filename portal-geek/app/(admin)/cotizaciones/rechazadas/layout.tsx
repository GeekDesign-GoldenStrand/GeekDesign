import type { Metadata } from "next";

export const metadata: Metadata = { title: "Cotizaciones rechazadas" };

export default function CotizacionesRechazadasLayout({ children }: { children: React.ReactNode }) {
  return children;
}
