import type { Metadata } from "next";

export const metadata: Metadata = { title: "Detalle de sucursal" };

export default function SucursalDetalleLayout({ children }: { children: React.ReactNode }) {
  return children;
}
