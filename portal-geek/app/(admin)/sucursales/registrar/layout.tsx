import type { Metadata } from "next";

export const metadata: Metadata = { title: "Registrar sucursal" };

export default function RegistrarSucursalLayout({ children }: { children: React.ReactNode }) {
  return children;
}
